#ifndef MAIN_H
#define MAIN_H


#include <stdarg.h>
#include <limits.h>
#include <stdio.h>
#include <unistd.h>

int _strln(char str[]);
int _putshar(char c);
int _printf(const char *format, ...);

#endif
