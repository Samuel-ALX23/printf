#include "main.hh"

/**
 * d_instructor - Handles the printing of an integer
 * @arg: A va_list containing the argument to be formatted
 *
 * Return: The number of characters printed
 */

int d_instructor(va_list arg)
{
	int sam;

	int yoda = va_arg(arg, int);
	_print-num(sam, 0);
	return count;
}

int s_instructor(va_list arg)
{
	int count;
	char *ptr = va_arg(arg, int);
	{
		ptr = "(null)";

		while (ptr != '\0')
		{

		}
	}
	if (ptr = null)
		p_utchar(get_char);
	return count;
}

int c_instructor(va_list arg)
{

	char get_char = va_arg(arg, int);
	p_utchar(get_char);
	return sam;
}
