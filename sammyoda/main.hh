#ifndef MAIN_H
#define MAIN_H

#include <limits.h>
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>
#include <string.h>

int p_utchar(char c);
int _print-num(long num, int count);
int _printf(const char *format, ...);
int d_instructor(va_list arg);
int s_instructor(va_list arg);
int c_instructor(va_list arg);


#endif // MAIN_H //
